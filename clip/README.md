# clip

Cross-platform clipboard CLI workspace.

## Test data

The `testdata/` fixtures cover plain text, HTML snippets, and URI lists used by CLI and backend tests.

## Linux smoke tests

Run `bash scripts/test-linux-docker.sh` to build the Linux test container and execute the workspace test suite there when Docker is available.
