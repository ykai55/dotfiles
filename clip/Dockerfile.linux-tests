FROM rust:1.87

WORKDIR /work

RUN apt-get update \
    && apt-get install -y --no-install-recommends bash pkg-config \
    && rm -rf /var/lib/apt/lists/*

COPY . .

CMD ["cargo", "test", "--workspace"]
